import { combineReducers } from "redux";

export default function(state = {}, action) {
  switch (action.type) {
    default:
      return state;
  }
}

// export default combineReducers({
//   api:
// })
